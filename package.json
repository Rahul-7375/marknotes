{
  "name": "marknotes",
  "version": "1.0.0",
  "description": "Full-stack Markdown Notes App — single server",
  "main": "server.js",
  "scripts": {
    "dev": "node server.js",
    "build": "cd client && npm install && npm run build",
    "start": "npm run build && npm run dev",
    "setup": "npm install && npm run build"
  },
  "dependencies": {
    "@supabase/ssr": "^0.10.2",
    "@supabase/supabase-js": "^2.103.0",
    "body-parser": "^1.20.2",
    "cors": "^2.8.5",
    "dotenv": "^17.4.2",
    "express": "^4.18.2",
    "express-validator": "^7.0.1",
    "pg": "^8.20.0"
  }
}
